# Шаг 7 — Тесты

1.* * Кодек * *: `explanation` присутствует / отсутствует; неизвестные поля по - прежнему игнорируются .
2.* * Application * *: селективный approve — применяются ровно выбранные; сводка об отклонённых попадает в префикс следующего сообщения; reject - by - typing чистит pending.Фейковый `CodeRepository`, без IDE .
3.* * Ручной smoke -чеклист * *: карточка(
    чекбоксы,
    Apply selected / all,
    Reject all,
    заморозка при наборе текста
), Diff по каждому типу правки, PSI - ссылки(валидная и битая).
